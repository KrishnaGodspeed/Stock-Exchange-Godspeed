# DELETE
http.delete./schema/wishlist/{id}:
  summary: Delete a WishList
  description: Delete WishList from database
  fn: com.biz.schema.wishlist.delete
  params:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    '202':
      content:
        application/json:
          schema:
            type: object

