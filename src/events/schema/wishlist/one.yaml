# ONE
http.get./schema/wishlist/{id}:
  summary: Fetch WishList
  description: Fetch WishList from database
  fn: com.biz.schema.wishlist.one
  params:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    '200':
      content:
        application/json:
          schema:
            type: object

