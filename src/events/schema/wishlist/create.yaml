# CREATE
http.post./schema/wishlist:
  summary: Create a new WishList
  description: Create WishList from database
  fn: com.biz.schema.wishlist.create
  body:
    content:
      application/json:
        schema:
          $ref: '#/definitions/schema/WishList'
  responses:
    '201':
      content:
        application/json:
          schema:
            type: object

