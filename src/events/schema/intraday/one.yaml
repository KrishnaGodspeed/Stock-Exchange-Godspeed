# ONE
http.get./schema/intraday/{id}:
  summary: Fetch Intraday
  description: Fetch Intraday from database
  fn: com.biz.schema.intraday.one
  params:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    '200':
      content:
        application/json:
          schema:
            type: object

